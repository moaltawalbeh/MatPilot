import { Page } from "@/components/ui/page"; import { mockService } from "@/services/mock-service"; import Link from "next/link"; import { Plus } from "lucide-react";
export default async function Projects(){const all=await mockService.projects();return <Page eyebrow="Workspace" title="Projects" description="Organize raw data, analyses, notes, and reports in one scientific record." actions={<button className="button primary"><Plus size={15}/>New project</button>}><div className="grid three">{all.map(p=><Link key={p.id} href={`/projects/${p.id}`} className="card" style={{textDecoration:'none',color:'inherit'}}><span className="badge">XRD</span><h2 style={{marginTop:23}}>{p.name}</h2><p className="muted">{p.material}</p><div style={{borderTop:'1px solid #263545',paddingTop:13,marginTop:18,display:'flex',justifyContent:'space-between'}}><span className="muted">{p.files} files · {p.analyses} analyses</span><span className={`badge ${p.status==='Complete'?'good':''}`}>{p.status}</span></div></Link>)}</div></Page>}
