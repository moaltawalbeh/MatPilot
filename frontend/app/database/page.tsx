"use client"; import { Page } from "@/components/ui/page"; import { matches } from "@/services/mock-service"; import { Search,SlidersHorizontal } from "lucide-react"; import { useState } from "react";
export default function Database(){const [query,setQuery]=useState('');const found=matches.filter(x=>(x.name+x.formula).toLowerCase().includes(query.toLowerCase()));return <Page eyebrow="Reference knowledge" title="Materials database" description="Search crystallographic references across connected scientific sources."><section className="card"><div className="composer"><Search size={16} style={{margin:'7px 0 0 7px'}}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search formula, material, or structure…"/><button><SlidersHorizontal size={14}/></button></div><div className="formats" style={{justifyContent:'start'}}>{['COD','Materials Project','OQMD','AFLOW','NOMAD','Materials Cloud'].map(x=><span className="badge" key={x}>{x}</span>)}</div></section><div className="grid three" style={{marginTop:16}}>{found.map(m=><section className="card" key={m.formula}><span className="badge">{m.source}</span><h2 style={{marginTop:18}}>{m.formula}</h2><p className="muted">{m.name}</p><div style={{borderTop:'1px solid #263545',paddingTop:13,marginTop:18,display:'flex',justifyContent:'space-between'}}><span className="muted">{m.system}</span><span className="good">{m.score}% match</span></div></section>)}</div></Page>}
