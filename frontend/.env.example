# ============================================
# MatPilot Frontend — Environment Variables
# ============================================

# Backend API URL (must include protocol, no trailing slash)
# In production, set this in Vercel → Settings → Environment Variables.
# For local development, point it at your local backend and set it in .env.local.
NEXT_PUBLIC_API_URL=https://matpilot.site
