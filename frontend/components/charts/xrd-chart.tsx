"use client"; import { CartesianGrid,Legend,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
const points=Array.from({length:71},(_,i)=>{const x=10+i;const p=(at:number,h:number,w:number)=>h*Math.exp(-Math.pow((x-at)/w,2));const calc=3+p(18.7,43,1.35)+p(37.5,100,1.7)+p(44.2,67,1.3)+p(48.5,54,1.3)+p(65,38,1.8);return{angle:x,Experimental:Math.round(calc+(i%5)),Calculated:Math.round(calc*.94),Difference:Math.round((i%5)-2)}});
export function XrdChart(){return <div className="chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={points} margin={{top:8,right:8,left:-18}}><CartesianGrid stroke="#263545" strokeDasharray="2 4" vertical={false}/><XAxis dataKey="angle" tick={{fontSize:10}} stroke="#8090a1" tickLine={false}/><YAxis tick={{fontSize:10}} stroke="#8090a1" tickLine={false}/><Tooltip contentStyle={{background:'#101a24',border:'1px solid #36516b',borderRadius:6,fontSize:11}}/><Legend wrapperStyle={{fontSize:11}}/><Line dataKey="Experimental" stroke="#70bdff" strokeWidth={2} dot={false}/><Line dataKey="Calculated" stroke="#60d2b5" strokeWidth={1.5} dot={false}/><Line dataKey="Difference" stroke="#8897a8" strokeWidth={1} dot={false}/></LineChart></ResponsiveContainer></div>}
